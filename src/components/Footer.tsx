import { theme } from "../theme"

const Footer = () => {
    return (
        <div style={{backgroundColor: theme.colors.green3, height: '80px'}}>
            footer
        </div>
    )
}

export default Footer