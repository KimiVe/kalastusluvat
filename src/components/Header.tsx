import { theme } from "../theme"

const Header = () => {
    return (
        <div style={{backgroundColor: theme.colors.green3, fontSize: '2em', height: '70px'}}>
            Kala-appi
        </div>
    )
}
export default Header